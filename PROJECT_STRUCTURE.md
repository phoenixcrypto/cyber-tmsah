# بنية المشروع - Project Structure

## 📁 البنية العامة

```
cyber-tmsah/
├── app/                          # Next.js App Router
│   ├── [[...theme]]/            # نظام الثيمات الديناميكي (Dynamic Theme Router)
│   │   ├── page.tsx             # نقطة دخول جميع صفحات الثيمات
│   │   └── layout.tsx            # Layout للثيمات
│   │
│   ├── admin/                   # 🎛️ لوحة التحكم الإدارية (Admin Panel)
│   │   ├── page.tsx             # Dashboard الرئيسي
│   │   ├── content/             # إدارة المحتوى
│   │   ├── users/                # إدارة المستخدمين
│   │   ├── themes/               # إدارة الثيمات
│   │   ├── design/               # إعدادات التصميم
│   │   └── settings/             # إعدادات عامة
│   │
│   ├── api/                     # 🔌 API Routes
│   │   ├── admin/                # APIs للإدارة
│   │   ├── auth/                 # APIs للمصادقة
│   │   ├── articles/             # APIs للمقالات
│   │   ├── materials/            # APIs للمواد
│   │   ├── schedule/             # APIs للجدول
│   │   └── themes/               # APIs للثيمات
│   │
│   ├── about/                    # ⚠️ صفحات قديمة (Legacy Pages)
│   ├── contact/                   # ⚠️ صفحات قديمة
│   ├── materials/                # ⚠️ صفحات قديمة
│   ├── schedule/                 # ⚠️ صفحات قديمة
│   └── downloads/                # ⚠️ صفحات قديمة
│
├── themes/                       # 🎨 نظام الثيمات (Theme System)
│   └── default/                  # الثيم الافتراضي
│       ├── pages/                # ✅ صفحات الموقع الحقيقية
│       │   ├── home.tsx          # الصفحة الرئيسية
│       │   ├── about/            # من نحن
│       │   ├── contact/          # اتصل بنا
│       │   ├── materials/        # المواد الدراسية
│       │   ├── schedule/         # الجدول الدراسي
│       │   └── downloads/        # التنزيلات
│       ├── components/           # مكونات الثيم
│       │   ├── Navbar.tsx        # شريط التنقل
│       │   ├── Footer.tsx        # التذييل
│       │   └── PageHeader.tsx    # رأس الصفحة
│       ├── layouts/               # Layouts
│       │   └── MainLayout.tsx    # Layout الرئيسي
│       └── styles/                # الأنماط
│           └── globals.css       # CSS العام
│
├── components/                   # 🧩 مكونات مشتركة
│   ├── admin/                    # مكونات لوحة التحكم
│   └── [shared components]       # مكونات مشتركة
│
├── lib/                          # 📚 المكتبات والأدوات
│   ├── theme/                    # نظام الثيمات
│   ├── content/                  # Content API
│   ├── db/                       # قاعدة البيانات
│   └── [other utilities]        # أدوات أخرى
│
└── platform/                    # 🏗️ المنصة الأساسية (قيد التطوير)
```

## 🎯 أين الموقع؟

### ✅ الموقع الحقيقي (Active Website):
**الموقع موجود في:** `themes/default/pages/`

- **الصفحة الرئيسية:** `themes/default/pages/home.tsx`
- **المواد الدراسية:** `themes/default/pages/materials/`
- **الجدول الدراسي:** `themes/default/pages/schedule/`
- **اتصل بنا:** `themes/default/pages/contact/`
- **من نحن:** `themes/default/pages/about/`

**كيف يعمل:**
1. المستخدم يزور `/` (الصفحة الرئيسية)
2. `app/[[...theme]]/page.tsx` يحدد الثيم النشط (default)
3. يحمل الصفحة من `themes/default/pages/home.tsx`
4. يعرضها مع `MainLayout` الذي يحتوي على Navbar و Footer

### ⚠️ الصفحات القديمة (Legacy - غير مستخدمة):
**موجودة في:** `app/about/`, `app/contact/`, `app/materials/`, إلخ

هذه الصفحات **قديمة** ويجب حذفها لأن الموقع الحقيقي موجود في `themes/default/pages/`

## 🎛️ أين المنصة الإدارية؟

**المنصة الإدارية موجودة في:** `app/admin/`

- **لوحة التحكم:** `app/admin/page.tsx`
- **إدارة المحتوى:** `app/admin/content/`
- **إدارة المستخدمين:** `app/admin/users/`
- **إدارة الثيمات:** `app/admin/themes/`
- **الإعدادات:** `app/admin/settings/`

**الوصول:**
- المسار: `/admin` (أو `/eltmsah` حسب الإعدادات)
- يتطلب تسجيل دخول

## 🔄 كيف يعمل النظام؟

### 1. نظام الثيمات (Theme System):
```
المستخدم يزور URL → app/[[...theme]]/page.tsx
  ↓
يحدد الثيم النشط (default)
  ↓
يحمل الصفحة من themes/default/pages/
  ↓
يعرضها مع MainLayout
```

### 2. لوحة التحكم (Admin Panel):
```
المستخدم يزور /admin → app/admin/layout.tsx
  ↓
يتحقق من المصادقة
  ↓
يعرض لوحة التحكم
```

### 3. API Routes:
```
العميل يطلب API → app/api/[route]/route.ts
  ↓
يتحقق من الصلاحيات
  ↓
يتفاعل مع قاعدة البيانات
  ↓
يرجع النتيجة
```

## 📝 ملاحظات مهمة:

1. **الصفحات القديمة:** `app/about/`, `app/contact/` إلخ يجب حذفها
2. **الموقع الحقيقي:** في `themes/default/pages/`
3. **إضافة صفحات جديدة:** أضفها في `themes/default/pages/`
4. **تغيير التصميم:** عدل في `themes/default/components/` و `themes/default/styles/`

## 🚀 الخطوات التالية:

1. ✅ حذف الصفحات القديمة من `app/`
2. ✅ التأكد من أن جميع الروابط تشير إلى صفحات الثيمات
3. ✅ اختبار جميع الصفحات

