# 🔑 كيفية إعادة تعيين Supabase Service Role Key

## 📍 **مكان زر Reset في Supabase**

### **الطريقة 1: من صفحة API Keys (الأسهل)**

1. اذهب إلى: **Supabase Dashboard** → مشروعك
2. من القائمة الجانبية: **Settings** (الإعدادات) → **API**
3. في قسم **"Project API keys"**:
   - ابحث عن **`service_role` secret key**
   - اضغط على زر **"Reveal"** لإظهار المفتاح الحالي
   - بعد إظهار المفتاح، قد يظهر زر **"Reset"** أو **"Regenerate"**
   - إذا لم يظهر، استخدم الطريقة 2

---

### **الطريقة 2: من JWT Settings (الأكيد)**

1. اذهب إلى: **Supabase Dashboard** → مشروعك
2. من القائمة الجانبية: **Settings** → **API**
3. ابحث عن قسم **"JWT Settings"** أو **"JWT Secret"**
4. في هذا القسم ستجد:
   - **"Generate new JWT secret"** أو **"Reset JWT Secret"**
   - اضغط عليه
   - ⚠️ **تحذير**: سيتم إعادة تعيين جميع المفاتيح (anon + service_role)

---

### **الطريقة 3: من Project Settings**

1. اذهب إلى: **Supabase Dashboard** → مشروعك
2. من القائمة الجانبية: **Settings** → **General**
3. ابحث عن قسم **"API Keys"** أو **"Security"**
4. قد تجد خيار لإعادة تعيين المفاتيح هناك

---

## ⚠️ **مهم جداً**

عند إعادة تعيين JWT Secret:
- ✅ سيتم إنشاء **مفاتيح جديدة** لـ `anon` و `service_role`
- ✅ المفاتيح **القديمة لن تعمل** بعد ذلك
- ✅ يجب تحديث **جميع** Environment Variables في Vercel:
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY` (جديد)
  - `SUPABASE_SERVICE_ROLE_KEY` (جديد)

---

## 📝 **خطوات مفصلة بعد إعادة التعيين**

### 1. بعد الضغط على Reset/Regenerate:

1. **انسخ المفتاح الجديد** فوراً (لن يظهر مرة أخرى!)
2. اذهب إلى **Vercel** → مشروعك → **Settings** → **Environment Variables**

### 2. حدّث المفاتيح في Vercel:

#### أ) `NEXT_PUBLIC_SUPABASE_ANON_KEY`:
- ابحث عن المتغير
- Edit
- الصق المفتاح الجديد من Supabase (anon key)
- Save

#### ب) `SUPABASE_SERVICE_ROLE_KEY`:
- ابحث عن المتغير
- Edit
- الصق المفتاح الجديد من Supabase (service_role key)
- Save

### 3. إعادة النشر:

1. Vercel Dashboard → آخر deployment
2. اضغط **"Redeploy"**
3. انتظر حتى ينتهي النشر

---

## 🎯 **الطريقة الموصى بها (الأسهل)**

### **بدلاً من Reset، استخدم Reveal ثم انسخ:**

1. اذهب إلى: **Settings** → **API**
2. ابحث عن **`service_role` secret key**
3. اضغط **"Reveal"**
4. انسخ المفتاح الكامل
5. إذا كان المفتاح الحالي مختلف عن الموجود في Vercel:
   - حدّثه في Vercel Environment Variables
   - Redeploy

### **إذا لم تجد زر Reset:**

- قد لا يكون موجوداً في واجهة Supabase الحالية
- الحل: استخدم **"Reveal"** ثم انسخ المفتاح
- إذا كان المفتاح مختلف، حدّثه في Vercel

---

## ✅ **التحقق من نجاح العملية**

بعد تحديث المفاتيح:

1. جرّب تسجيل الدخول: https://www.cyber-tmsah.site/login
2. إذا عمل بشكل طبيعي → ✅ نجح
3. إذا ظهر خطأ → تحقق من:
   - المفاتيح محدثة في Vercel
   - تم Redeploy
   - المفاتيح صحيحة (نسختها بشكل صحيح)

---

## 🆘 **إذا لم تجد زر Reset**

**لا مشكلة!** يمكنك:
1. استخدام المفتاح الحالي (إذا كان آمناً)
2. أو إنشاء مشروع Supabase جديد ونقل البيانات
3. أو الاتصال بدعم Supabase لطلب إعادة التعيين

---

**ملاحظة**: في بعض إصدارات Supabase، زر Reset قد يكون في مكان مختلف. جرب الطرق الثلاث أعلاه.

