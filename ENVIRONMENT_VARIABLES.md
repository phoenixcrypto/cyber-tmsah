# 📋 Environment Variables - دليل المتغيرات البيئية

## ✅ المتغيرات المستخدمة في المشروع

### 1. **DATABASE_URL** ✅ **مطلوب**
**الاستخدام:**
- الاتصال بقاعدة البيانات MySQL
- مستخدم في: `lib/db/prisma.ts`, `app/api/auth/login/route.ts`, `scripts/reset-admin.ts`

**القيمة:**
```
mysql://username:password@host:3306/database?sslaccept=strict
```

---

### 2. **ADMIN_PATH** ✅ **مستخدم (اختياري)**
**الاستخدام:**
- تحديد مسار لوحة التحكم (للأمان)
- مستخدم في: `lib/utils/admin-path.ts`, `next.config.js`
- القيمة الافتراضية: `admin`

**مثال:**
```
ADMIN_PATH=dashboard
```
سيجعل لوحة التحكم في: `/dashboard` بدلاً من `/admin`

---

### 3. **JWT_SECRET** ✅ **مطلوب**
**الاستخدام:**
- توقيع JWT tokens للمستخدمين
- مستخدم في: `lib/auth/jwt.ts`, `app/api/auth/login/route.ts`

**مثال:**
```bash
# يمكن إنشاؤه بهذا الأمر:
openssl rand -base64 32
```

---

### 4. **JWT_REFRESH_SECRET** ✅ **مطلوب**
**الاستخدام:**
- توقيع Refresh tokens
- مستخدم في: `lib/auth/jwt.ts`, `app/api/auth/login/route.ts`

**مثال:**
```bash
# يمكن إنشاؤه بهذا الأمر:
openssl rand -base64 32
```

---

### 5. **DEFAULT_ADMIN_USERNAME** ✅ **مطلوب**
**الاستخدام:**
- اسم المستخدم الافتراضي للـ admin
- يستخدم عند إنشاء admin تلقائياً إذا لم يكن موجوداً
- مستخدم في: `app/api/auth/login/route.ts`, `scripts/reset-admin.ts`, `app/api/auth/me/route.ts`

**مثال:**
```
DEFAULT_ADMIN_USERNAME=zeyadeltmsah26
```

---

### 6. **DEFAULT_ADMIN_PASSWORD** ✅ **مطلوب**
**الاستخدام:**
- كلمة مرور الـ admin الافتراضية
- يستخدم عند إنشاء admin تلقائياً
- مستخدم في: `app/api/auth/login/route.ts`, `scripts/reset-admin.ts`

**مثال:**
```
DEFAULT_ADMIN_PASSWORD=2610204ZEYAd@@
```

⚠️ **تنبيه:** غيّرها بعد أول تسجيل دخول!

---

### 7. **DEFAULT_ADMIN_NAME** ✅ **مستخدم (اختياري)**
**الاستخدام:**
- اسم عرض الـ admin (في لوحة التحكم)
- مستخدم في: `app/api/auth/login/route.ts`, `scripts/reset-admin.ts`, `app/api/auth/me/route.ts`
- القيمة الافتراضية: نفس `DEFAULT_ADMIN_USERNAME` إذا لم يتم تعيينه

**مثال:**
```
DEFAULT_ADMIN_NAME=Admin User
```

---

### 8. **RESEND_API_KEY** ✅ **مستخدم (اختياري لكن موصى به)**
**الاستخدام:**
- إرسال الإيميلات من صفحة "اتصل بنا"
- مستخدم في: `app/api/contact/route.ts`
- بدون هذا المتغير، صفحة "اتصل بنا" لن تعمل

**الحصول عليه:**
1. سجل في: https://resend.com/
2. أنشئ API Key
3. أضفه هنا

**مثال:**
```
RESEND_API_KEY=re_xxxxxxxxxxxxx
```

---

### 9. **CONTACT_EMAIL** ✅ **مستخدم (اختياري)**
**الاستخدام:**
- البريد الإلكتروني الذي يتم إرسال رسائل "اتصل بنا" إليه
- مستخدم في: `app/api/contact/route.ts`
- القيمة الافتراضية: `info@cyber-tmsah.site`

**مثال:**
```
CONTACT_EMAIL=contact@yourdomain.com
```

---

### 10. **FROM_EMAIL** ✅ **مستخدم (اختياري)**
**الاستخدام:**
- البريد الإلكتروني المرسل منه (في رسائل "اتصل بنا")
- مستخدم في: `app/api/contact/route.ts`
- القيمة الافتراضية: `Cyber TMSAH <noreply@cyber-tmsah.site>`

**مثال:**
```
FROM_EMAIL=Cyber TMSAH <noreply@yourdomain.com>
```

⚠️ **ملاحظة:** يجب أن يكون verified في Resend

---

### 11. **NODE_ENV** ⚠️ **يتم إضافته تلقائياً**
**الاستخدام:**
- تحديد بيئة التطبيق (development/production)
- مستخدم في عدة ملفات للتحقق من البيئة
- **لا حاجة لإضافته يدوياً** - Vercel يضيفه تلقائياً

**القيم:**
- `production` - على Vercel
- `development` - محلياً

---

## 📊 ملخص المتغيرات

| المتغير | الحالة | مطلوب؟ | القيمة الافتراضية |
|---------|--------|--------|-------------------|
| `DATABASE_URL` | ✅ مستخدم | نعم | - |
| `ADMIN_PATH` | ✅ مستخدم | لا | `admin` |
| `JWT_SECRET` | ✅ مستخدم | نعم | - |
| `JWT_REFRESH_SECRET` | ✅ مستخدم | نعم | - |
| `DEFAULT_ADMIN_USERNAME` | ✅ مستخدم | نعم | - |
| `DEFAULT_ADMIN_PASSWORD` | ✅ مستخدم | نعم | - |
| `DEFAULT_ADMIN_NAME` | ✅ مستخدم | لا | `DEFAULT_ADMIN_USERNAME` |
| `RESEND_API_KEY` | ✅ مستخدم | لا* | - |
| `CONTACT_EMAIL` | ✅ مستخدم | لا | `info@cyber-tmsah.site` |
| `FROM_EMAIL` | ✅ مستخدم | لا | `Cyber TMSAH <noreply@cyber-tmsah.site>` |
| `NODE_ENV` | ⚠️ تلقائي | لا | Vercel يضيفه |

*مطلوب فقط إذا كنت تريد عمل صفحة "اتصل بنا"

---

## ✅ التوصية النهائية

### متغيرات **يجب** إضافتها:
- ✅ `DATABASE_URL`
- ✅ `JWT_SECRET`
- ✅ `JWT_REFRESH_SECRET`
- ✅ `DEFAULT_ADMIN_USERNAME`
- ✅ `DEFAULT_ADMIN_PASSWORD`

### متغيرات **موصى بها**:
- ✅ `RESEND_API_KEY` (لصفحة "اتصل بنا")
- ✅ `CONTACT_EMAIL` (إذا كنت تريد بريد مختلف)
- ✅ `FROM_EMAIL` (إذا كنت تريد بريد مختلف)

### متغيرات **اختيارية**:
- ⚪ `ADMIN_PATH` (إذا كنت تريد تغيير مسار لوحة التحكم)
- ⚪ `DEFAULT_ADMIN_NAME` (إذا كنت تريد اسم عرض مختلف)

### متغيرات **لا حاجة لها**:
- ❌ `NODE_ENV` (يتم إضافته تلقائياً من Vercel)

---

## 🔒 الأمان

1. ✅ لا تشارك Environment Variables علناً
2. ✅ استخدم كلمات مرور قوية
3. ✅ غيّر `DEFAULT_ADMIN_PASSWORD` بعد أول تسجيل دخول
4. ✅ استخدم JWT secrets عشوائية قوية
5. ✅ تأكد من أن `DATABASE_URL` يحتوي على `sslaccept=strict`

