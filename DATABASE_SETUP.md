# إعداد قاعدة البيانات MySQL

## ⚠️ ملاحظة مهمة: MySQL المحلي vs MySQL السحابي

### ❌ MySQL المحلي (Local MySQL)
**لا يمكن استخدامه للنشر على Vercel!**

- MySQL المحلي يعمل فقط على جهازك
- Vercel لا يمكنه الوصول إلى قاعدة البيانات على جهازك الشخصي
- يحتاج إلى اتصال إنترنت عام ومفتوح (غير آمن)

### ✅ MySQL السحابي (Cloud MySQL)
**هذا ما تحتاجه للنشر!**

يمكنك استخدام إحدى الخدمات التالية:

---

## 1. PlanetScale (موصى به) 🌟

### المميزات:
- ✅ مجاني للبدء (5 GB storage)
- ✅ سريع جداً
- ✅ سهل الإعداد
- ✅ يعمل مع Prisma بشكل رائع
- ✅ نسخ احتياطي تلقائي

### خطوات الإعداد:
1. سجل في [PlanetScale](https://planetscale.com/)
2. أنشئ قاعدة بيانات جديدة
3. اختر Region قريب (مثلاً: Middle East)
4. انسخ Connection String
5. الصقه في Vercel Environment Variables كـ `DATABASE_URL`

### مثال على DATABASE_URL:
```
mysql://username:password@host.planetscale.com/database?sslaccept=strict
```

---

## 2. AWS RDS MySQL

### المميزات:
- ✅ موثوق جداً
- ✅ قابل للتوسع
- ✅ خيارات أمان متقدمة

### العيوب:
- ❌ معقد الإعداد قليلاً
- ❌ قد يكون مكلفاً

---

## 3. DigitalOcean Managed MySQL

### المميزات:
- ✅ سهل الاستخدام
- ✅ أسعار معقولة
- ✅ أداء جيد

---

## 4. Supabase (PostgreSQL - **لا يستخدم MySQL**)

⚠️ **تنبيه**: Supabase يستخدم PostgreSQL وليس MySQL. إذا كنت تريد استخدام Supabase، ستحتاج إلى تغيير Prisma Schema من `mysql` إلى `postgresql`.

---

## الإعداد على Vercel

بعد إنشاء قاعدة البيانات السحابية:

1. اذهب إلى Vercel Dashboard → Project → Settings → Environment Variables
2. أضف `DATABASE_URL` مع رابط قاعدة البيانات
3. تأكد من إضافة `?sslaccept=strict` في النهاية للأمان
4. أضف المتغيرات الأخرى:
   - `DEFAULT_ADMIN_USERNAME`
   - `DEFAULT_ADMIN_PASSWORD`
   - `JWT_SECRET`
   - `JWT_REFRESH_SECRET`
5. أعد نشر المشروع

---

## مثال كامل لـ DATABASE_URL

### PlanetScale:
```
mysql://root:password@host.planetscale.com/database_name?sslaccept=strict
```

### AWS RDS:
```
mysql://username:password@database-name.region.rds.amazonaws.com:3306/database_name?sslaccept=strict
```

### DigitalOcean:
```
mysql://username:password@host.db.ondigitalocean.com:25060/database_name?sslaccept=strict
```

---

## اختبار الاتصال محلياً

يمكنك اختبار الاتصال محلياً باستخدام:

```bash
# إنشاء ملف .env.local
DATABASE_URL="mysql://username:password@host/database?sslaccept=strict"

# اختبار الاتصال
npm run db:push
```

---

## نصائح الأمان 🔒

1. ✅ استخدم SSL دائماً في Production (`?sslaccept=strict`)
2. ✅ لا تشارك `DATABASE_URL` علناً
3. ✅ استخدم كلمات مرور قوية
4. ✅ شغّل النسخ الاحتياطي التلقائي
5. ✅ حدّث كلمات المرور بانتظام

---

## للمساعدة

إذا واجهت أي مشاكل:
1. تحقق من أن `DATABASE_URL` يبدأ بـ `mysql://` أو `mysqlx://`
2. تأكد من أن قاعدة البيانات متاحة من الإنترنت
3. تحقق من firewall rules
4. راجع logs على Vercel

