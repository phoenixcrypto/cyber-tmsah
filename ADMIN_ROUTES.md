# 📋 قائمة روابط لوحة الادمن - Cyber TMSAH

## 🏠 الصفحات الرئيسية (Frontend Pages)

### 1. `/admin` - لوحة التحكم الرئيسية
**الوظيفة:** الصفحة الرئيسية للادمن - تعرض جميع الخيارات المتاحة
- عرض قائمة بجميع صفحات الادمن
- روابط سريعة للصفحات المهمة
- واجهة نظيفة ومنظمة

---

### 2. `/admin/verification` - رفع قائمة التحقق
**الوظيفة:** رفع ملف Excel/CSV يحتوي على بيانات الطلاب للتحقق
- رفع ملف Excel أو CSV
- استيراد بيانات الطلاب (703 طالب)
- التحقق من صحة البيانات قبل الاستيراد
- عرض نتائج الاستيراد

**API المستخدم:**
- `POST /api/admin/verification/upload` - رفع الملف

---

### 3. `/admin/verification-list` - إدارة قائمة التحقق
**الوظيفة:** عرض وإدارة جميع الطلاب في قائمة التحقق (703 طالب)
- عرض جميع الطلاب مع تفاصيلهم
- البحث والتصفية (بالاسم، القسم، المجموعة)
- تصفية حسب حالة التسجيل (مسجل/غير مسجل)
- عرض عدد المسجلين وغير المسجلين
- تصدير البيانات إلى CSV

**API المستخدم:**
- `GET /api/admin/verification/list` - جلب قائمة الطلاب

---

### 4. `/admin/students` - قائمة الطلاب المسجلين
**الوظيفة:** عرض جميع الطلاب المسجلين في النظام
- عرض جميع الطلاب المسجلين مع تفاصيلهم الكاملة
- إحصائيات شاملة:
  - إجمالي الطلاب المسجلين
  - دخلوا في آخر 24 ساعة
  - دخلوا في آخر 7 أيام
  - جدد في آخر 30 يوم
- البحث والتصفية (بالاسم، المستخدم، البريد الإلكتروني، القسم، المجموعة)
- تصدير البيانات إلى CSV
- عرض آخر دخول لكل طالب

**API المستخدم:**
- `GET /api/admin/students` - جلب قائمة الطلاب المسجلين

---

### 5. `/admin/content/publish` - نشر المحتوى
**الوظيفة:** نشر مقالات ومهام للطلاب
- نشر مقالات (Articles)
- نشر مهام (Tasks)
- تحديد الجمهور المستهدف:
  - عام (لجميع الطلاب)
  - محدد (حسب القسم/المجموعة)
- رفع ملفات مرفقة (PDF, DOCX, إلخ)
- إرسال إشعارات فورية أو مجدولة
- تحديد موعد الاستحقاق للمهام

**API المستخدم:**
- `POST /api/admin/content/publish/article` - نشر مقال
- `POST /api/admin/content/publish/task` - نشر مهمة
- `POST /api/admin/storage/upload` - رفع الملفات

---

### 6. `/admin/settings` - إعدادات الادمن
**الوظيفة:** إدارة إعدادات حساب الادمن
- عرض معلومات الحساب (الاسم، البريد الإلكتروني، اسم المستخدم)
- تغيير كلمة المرور
- تحديث المعلومات الشخصية

**API المستخدم:**
- `GET /api/admin/profile` - جلب معلومات الحساب
- `POST /api/admin/settings` - تحديث الإعدادات

---

## 🔌 API Routes (Backend Endpoints)

### التحقق والصلاحيات

#### `GET /api/admin/verify`
**الوظيفة:** التحقق من صلاحيات الادمن
- التحقق من أن المستخدم الحالي هو ادمن
- التحقق من أن الادمن نشط في قاعدة البيانات
- إرجاع معلومات الادمن

**الاستخدام:** يستخدم في صفحات الادمن للتحقق من الصلاحيات

---

### إدارة قائمة التحقق

#### `POST /api/admin/verification/upload`
**الوظيفة:** رفع ملف Excel/CSV لقائمة التحقق
- استقبال ملف Excel أو CSV
- قراءة البيانات من الملف
- إضافة/تحديث بيانات الطلاب في `verification_list`
- التحقق من صحة البيانات

**المعاملات:**
- `file`: ملف Excel/CSV

---

#### `GET /api/admin/verification/list`
**الوظيفة:** جلب قائمة جميع الطلاب من `verification_list`
- جلب جميع الطلاب (703 طالب)
- إرجاع تفاصيل كل طالب
- إحصائيات (مسجل/غير مسجل)

**الاستخدام:** يستخدم في صفحة `/admin/verification-list`

---

#### `POST /api/admin/verification/update`
**الوظيفة:** تحديث بيانات طالب في قائمة التحقق
- تحديث اسم الطالب
- تحديث القسم
- تحديث المجموعة
- تحديث حالة التسجيل

**المعاملات:**
- `id`: معرف الطالب
- `full_name`: الاسم الكامل
- `section_number`: رقم القسم
- `group_name`: اسم المجموعة

---

### إدارة الطلاب

#### `GET /api/admin/students`
**الوظيفة:** جلب قائمة جميع الطلاب المسجلين
- جلب جميع الطلاب من جدول `users` حيث `role = 'student'`
- إرجاع تفاصيل كل طالب
- إحصائيات شاملة:
  - إجمالي الطلاب
  - دخلوا في آخر 24 ساعة
  - دخلوا في آخر 7 أيام
  - جدد في آخر 30 يوم
  - توزيع حسب القسم
  - توزيع حسب المجموعة

**الاستخدام:** يستخدم في صفحة `/admin/students`

---

### نشر المحتوى

#### `POST /api/admin/content/publish/article`
**الوظيفة:** نشر مقال جديد
- إنشاء مقال في قاعدة البيانات
- تحديد الجمهور المستهدف (عام أو محدد)
- ربط الملفات المرفقة
- إرسال إشعارات (اختياري)

**المعاملات:**
- `title`: عنوان المقال
- `description`: وصف المقال
- `content`: محتوى المقال
- `subjectId`: معرف المادة (اختياري)
- `isGeneral`: هل المقال عام (لجميع الطلاب)
- `targetSections`: الأقسام المستهدفة (إذا لم يكن عام)
- `targetGroups`: المجموعات المستهدفة (إذا لم يكن عام)
- `files`: روابط الملفات المرفقة
- `sendNotification`: إرسال إشعار (true/false)

---

#### `POST /api/admin/content/publish/task`
**الوظيفة:** نشر مهمة جديدة
- إنشاء مهمة في قاعدة البيانات
- تحديد موعد الاستحقاق
- تحديد الجمهور المستهدف
- ربط الملفات المرفقة
- إرسال إشعارات (اختياري)

**المعاملات:**
- `title`: عنوان المهمة
- `description`: وصف المهمة
- `content`: محتوى المهمة
- `subjectId`: معرف المادة (اختياري)
- `dueDate`: موعد الاستحقاق
- `isGeneral`: هل المهمة عامة
- `targetSections`: الأقسام المستهدفة
- `targetGroups`: المجموعات المستهدفة
- `files`: روابط الملفات المرفقة
- `sendNotification`: إرسال إشعار

---

### الإشعارات

#### `POST /api/admin/notifications/send`
**الوظيفة:** إرسال إشعارات للمحتوى
- إرسال إشعارات بريد إلكتروني للمقالات أو المهام
- فلترة المستلمين حسب إعدادات الإشعارات
- تحديد الجمهور المستهدف

**المعاملات:**
- `type`: نوع المحتوى ('article' أو 'task')
- `id`: معرف المقال أو المهمة
- `sendNow`: إرسال فوري (true/false)

---

#### `POST /api/admin/notifications/reminders`
**الوظيفة:** إرسال تذكيرات للمهام
- إرسال تذكيرات للمهام القريبة من موعد الاستحقاق
- فلترة حسب إعدادات الإشعارات

**المعاملات:**
- `taskId`: معرف المهمة (اختياري - إذا لم يُحدد، يرسل لجميع المهام)

---

### إدارة الملفات

#### `POST /api/admin/storage/upload`
**الوظيفة:** رفع ملفات إلى Supabase Storage
- رفع ملفات متعددة
- تحديد المجلد (articles أو tasks)
- إرجاع روابط الملفات المرفوعة

**المعاملات:**
- `files`: ملفات متعددة (FormData)
- `folder`: اسم المجلد ('articles' أو 'tasks' أو 'general')

**الحد الأقصى:** 10MB لكل ملف

---

#### `POST /api/admin/upload`
**الوظيفة:** رفع ملفات عامة (قديمة - يُفضل استخدام `/api/admin/storage/upload`)

---

### إعدادات الادمن

#### `GET /api/admin/profile`
**الوظيفة:** جلب معلومات حساب الادمن
- إرجاع معلومات الحساب (الاسم، البريد، اسم المستخدم)

**الاستخدام:** يستخدم في صفحة `/admin/settings`

---

#### `POST /api/admin/settings`
**الوظيفة:** تحديث إعدادات حساب الادمن
- تحديث الاسم الكامل
- تحديث البريد الإلكتروني
- تغيير كلمة المرور

**المعاملات:**
- `fullName`: الاسم الكامل (اختياري)
- `email`: البريد الإلكتروني (اختياري)
- `currentPassword`: كلمة المرور الحالية (للتأكيد)
- `newPassword`: كلمة المرور الجديدة (اختياري)

---

## 🔐 الحماية والأمان

جميع الروابط محمية بـ:
- ✅ **JWT Authentication**: التحقق من الـ token
- ✅ **Admin Role Check**: التحقق من أن المستخدم ادمن
- ✅ **Active Status Check**: التحقق من أن الادمن نشط في قاعدة البيانات
- ✅ **Rate Limiting**: حماية من الهجمات

---

## 📊 ملخص الروابط

### الصفحات (6 صفحات):
1. `/admin` - لوحة التحكم الرئيسية
2. `/admin/verification` - رفع قائمة التحقق
3. `/admin/verification-list` - إدارة قائمة التحقق
4. `/admin/students` - قائمة الطلاب المسجلين
5. `/admin/content/publish` - نشر المحتوى
6. `/admin/settings` - إعدادات الادمن

### API Routes (13 endpoint):
1. `GET /api/admin/verify` - التحقق من الصلاحيات
2. `POST /api/admin/verification/upload` - رفع ملف التحقق
3. `GET /api/admin/verification/list` - جلب قائمة التحقق
4. `POST /api/admin/verification/update` - تحديث بيانات التحقق
5. `GET /api/admin/students` - جلب قائمة الطلاب
6. `POST /api/admin/content/publish/article` - نشر مقال
7. `POST /api/admin/content/publish/task` - نشر مهمة
8. `POST /api/admin/notifications/send` - إرسال إشعارات
9. `POST /api/admin/notifications/reminders` - إرسال تذكيرات
10. `POST /api/admin/storage/upload` - رفع ملفات
11. `POST /api/admin/upload` - رفع ملفات (قديم)
12. `GET /api/admin/profile` - معلومات الحساب
13. `POST /api/admin/settings` - تحديث الإعدادات

---

## 📝 ملاحظات مهمة

1. **جميع الروابط محمية** - تتطلب تسجيل دخول كادمن
2. **Rate Limiting** - معظم API routes لديها حماية من الإفراط في الاستخدام
3. **File Upload** - الحد الأقصى 10MB لكل ملف
4. **Notifications** - يمكن فلترة المستلمين حسب إعدادات الإشعارات
5. **Storage** - الملفات تُرفع إلى Supabase Storage في bucket اسمه `content-files`

---

**آخر تحديث:** 2025-01-09

